<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Post</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
  </head>
  <body>
    <div class="container">
      <div class="row justify-content-md-center">
        <div class="col col-lg-2">
          <h1><a href="/">Blog</a></h1>

          <% flash.keys.each do |type| %>
            <div data-alert class="flash alert alert-danger alert-box radius">
              <%= flash[type] %>
            </div>
          <% end %>

          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
